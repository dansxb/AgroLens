# ============================================================
# AgroLens Backend — Python Dependencies
# All versions pinned for reproducible builds.
# Update with: pip-compile requirements.in
# ============================================================

# --- Web Framework ---
fastapi==0.111.*
uvicorn[standard]==0.29.*

# --- Database (ORM + migrations + drivers) ---
sqlalchemy==2.0.*
geoalchemy2==0.15.*
alembic==1.13.*
psycopg2-binary==2.9.*
asyncpg==0.29.*          # Async PostgreSQL driver for SQLAlchemy async engine

# --- Settings / Config ---
pydantic==2.7.*
pydantic-settings==2.2.*
python-dotenv==1.0.*

# --- Authentication ---
python-jose[cryptography]==3.3.*
passlib[bcrypt]==1.7.*
httpx==0.27.*            # Async HTTP client (used for JWKS fetching)

# --- Geospatial / Imagery Processing ---
rasterio==1.3.*
geopandas==0.14.*
shapely==2.0.*
pyproj==3.6.*
numpy==1.26.*
pillow==10.3.*

# --- Machine Learning ---
scikit-learn==1.4.*
torch==2.2.*

# --- Satellite Imagery ---
sentinelhub==3.10.*

# --- Background Tasks ---
celery[redis]==5.3.*
redis==5.0.*             # Redis Python client for rate limiting

# --- Cloud Storage ---
boto3==1.34.*

# --- Billing ---
stripe==8.9.*

# --- Notifications ---
sendgrid==6.11.*

# --- Rate Limiting ---
slowapi==0.1.*

# --- Error Tracking ---
sentry-sdk[fastapi]==1.45.*

# --- Export ---
reportlab==4.1.*         # PDF report generation
fiona==1.9.*             # Shapefile I/O (used by geopandas)
matplotlib==3.8.*        # Zone map rendering for PDF reports
