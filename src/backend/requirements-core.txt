# ============================================================
# AgroLens Backend — Core Dependencies (Phase 0 & 1)
# Used during Docker builds until Phase 2 geospatial work begins.
# Heavy ML/geospatial packages (torch, rasterio, geopandas, fiona)
# are in requirements.txt and added back when Phase 2 starts.
# ============================================================

# --- Web Framework ---
fastapi==0.111.*
uvicorn[standard]==0.29.*

# --- Database (ORM + migrations + drivers) ---
sqlalchemy==2.0.*
geoalchemy2==0.15.*
alembic==1.13.*
psycopg2-binary==2.9.*
asyncpg==0.29.*

# --- Settings / Config ---
pydantic==2.7.*
pydantic-settings==2.2.*
python-dotenv==1.0.*

# --- Authentication ---
python-jose[cryptography]==3.3.*
passlib[bcrypt]==1.7.*
httpx==0.27.*

# --- Geospatial (lightweight — no GDAL required) ---
shapely==2.0.*
numpy==1.26.*

# --- Background Tasks ---
celery[redis]==5.3.*
redis==5.0.*

# --- Cloud Storage ---
boto3==1.34.*

# --- Billing ---
stripe==8.9.*

# --- Notifications ---
sendgrid==6.11.*

# --- Error Tracking ---
sentry-sdk[fastapi]==1.45.*

# --- Export ---
reportlab==4.1.*
pillow==10.3.*
